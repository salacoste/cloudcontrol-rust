descption:  default config
# Database configuration (switched to SQLite, MongoDB not needed)
db_configs:
  type: sqlite
  db_name: cloudcontrol.db
  # The following MongoDB config is deprecated, kept for reference only
  # url: mongodb://127.0.0.1:27017
  # host: 127.0.0.1
  # port: 27017
  user:
  passwd:
# Redis configuration
redis_configs:
  redis_url: redis://:BIgsfintech27581@172.17.0.168:6379/15
#db_configs:
#  type: mongo
#  host: 127.0.0.1
#  port: 27017
#  #    user : admin
#  #    user1 : admin
#  #    passwd : Mongo2018914
#  db_name: online_data
#  db_name1: spider_data
# External service configuration
  db_name1: spider_data

# Kafka message queue
kafka_configs:
  topic: lang
  # Kafka server, usually a cluster address, single IP also works
  bootstrap_servers: "172.17.0.212:9090"
  # Consumer group ID must be unique, consumers in the same group do not consume duplicate data
  group_id: group_lang

rest_server_configs:
  host: 0.0.0.0
  port: 55555
  debug: True
#influxdb
influxdb_configs:
  host: 172.17.0.212
#  host: 127.0.0.1
  port: 8086
  user: admin
  passwd: adminATinfluxdb2016
  db_name: op_spider

# Spider configuration
SPIDER:
  MIDDLEWARE: "common.middleware.RequestsMiddleware"


# Server port
server:
  port: 8000